$('#favorite-btn_<%= @post.id %>').html("<%= j(render "favorites/favorite-btn", post: @post) %>");
$('#favorite-count_<%= @post.id %>').html("<%= j(render "favorites/favorite-count", post: @post) %>");
